import React, { PropTypes } from "react";

const Footer = props => {
  return (
    <footer role="contentinfo">
      <p>This is my footer</p>
    </footer>
  )
};

Footer.propTypes = {};

export default Footer;
